import java.io.IOException;
import java.net.Socket;

/*
public class ThisIsRunnable implements Runnable{

    private Player1 player;
    private Socket socket;
    private GameServer game;

    public ThisIsRunnable (*/
/*Player player*//*
 Socket socket, GameServer game){
        //this.player = player;
        this.game = game;
        this.player = new Player1(socket, game);
        this.socket = socket;

    }

    @Override
    public void run() {
        try {

           //this.player = new Player(socket);
           player.openStreams();
           game.addPlayer(player);


        } catch (IOException ioException) {
            ioException.printStackTrace();
        }
    }

    public Player1 getPlayer() {
        return player;
    }

}
*/
